{
  "name": "horriblesubs-api",
  "version": "1.0.8",
  "description": "A horriblesubs.info API wrapper for NodeJS.",
  "dependencies": {
    "cheerio": "^1.0.0-rc.2",
    "debug": "^3.1.0",
    "got": "^7.1.0",
    "p-map": "^1.2.0",
    "p-whilst": "^1.0.0"
  },
  "devDependencies": {
    "chai": "^4.1.2",
    "coveralls": "^3.0.0",
    "eslint": "^4.11.0",
    "eslint-config-vixo": "^2.0.1",
    "mocha": "^4.0.1",
    "nyc": "^11.3.0"
  },
  "license": "MIT",
  "scripts": {
    "coveralls": "cat ./coverage/lcov.info | node node_modules/coveralls/bin/coveralls",
    "lint": "./node_modules/.bin/eslint {examples,test}/** *.js",
    "test": "./node_modules/.bin/nyc --reporter=lcov --reporter=text ./node_modules/.bin/mocha --timeout 30000 --reporter spec test/**"
  },
  "engines": {
    "node": ">=6.3.0"
  },
  "keywords": [
    "horriblesubs",
    "anime",
    "torrent",
    "torrents",
    "api"
  ],
  "main": "index.js",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/ChrisAlderson/horriblesubs-api.git"
  }
}
